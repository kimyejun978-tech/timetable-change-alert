name: Validate 오늘체육 MVP

on:
  push:
    branches: [feat/oneul-pe-mvp]
  pull_request:
    branches: [main]
    paths:
      - 'today-pe/**'
      - 'api/**'
      - 'tests/**'
      - 'package.json'
      - 'playwright.config.js'
      - 'vercel.json'
      - '.github/workflows/oneul-pe-validate.yml'
  workflow_dispatch:

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install app dependencies
        run: npm install --ignore-scripts --no-package-lock

      - name: JavaScript syntax check
        run: |
          find today-pe api tests -name '*.js' -print0 | while IFS= read -r -d '' file; do
            echo "checking $file"
            node --check "$file"
          done
          node --check playwright.config.js

      - name: Validate portal structure
        run: |
          python3 - <<'PY'
          from html.parser import HTMLParser
          from pathlib import Path
          import re

          pairs = [
              ('today-pe/student.html', 'today-pe/student.js'),
              ('today-pe/teacher-login.html', 'today-pe/teacher-login.js'),
              ('today-pe/teacher.html', 'today-pe/teacher.js'),
          ]

          class Parser(HTMLParser):
              def __init__(self):
                  super().__init__(); self.ids = []
              def handle_starttag(self, tag, attrs):
                  attrs = dict(attrs)
                  if 'id' in attrs: self.ids.append(attrs['id'])

          for html_path, js_path in pairs:
              html = Path(html_path).read_text(encoding='utf-8')
              js = Path(js_path).read_text(encoding='utf-8')
              p = Parser(); p.feed(html)
              dup = sorted({x for x in p.ids if p.ids.count(x) > 1})
              if dup: raise SystemExit(f'{html_path}: duplicate ids {dup}')
              refs = set(re.findall(r"getElementById\(['\"]([^'\"]+)['\"]\)", js))
              missing = sorted(refs - set(p.ids))
              if missing: raise SystemExit(f'{js_path}: missing ids {missing}')

          required = [
              'today-pe/index.html', 'today-pe/student.html', 'today-pe/student.js',
              'today-pe/student-privacy.js', 'today-pe/student-resilience.js',
              'today-pe/teacher-login.html', 'today-pe/teacher-login.js',
              'today-pe/teacher-approval-context.js', 'today-pe/teacher.html',
              'today-pe/teacher.js', 'today-pe/teacher-session-guard.js',
              'today-pe/teacher-dashboard-controls.js', 'today-pe/teacher-admin-protection.js',
              'today-pe/shared.js', 'today-pe/backend.js', 'today-pe/config.js',
              'today-pe/pwa.js', 'today-pe/push-client.js', 'today-pe/sw.js',
              'today-pe/manifest.webmanifest', 'today-pe/icon.svg',
              'today-pe/style.css', 'today-pe/student-extra.css', 'today-pe/teacher-extra.css',
              'today-pe/supabase/schema.sql',
              'today-pe/supabase/002_teacher_realtime.sql',
              'today-pe/supabase/003_collaboration_history_notifications.sql',
              'today-pe/supabase/004_push_subscriptions.sql',
              'today-pe/supabase/005_function_acl_hardening.sql',
              'today-pe/supabase/006_enable_pe_lessons_realtime.sql',
              'today-pe/supabase/007_lock_trigger_helper.sql',
              'today-pe/supabase/008_performance_hardening.sql',
              'today-pe/supabase/009_teacher_timetable_aliases.sql',
              'today-pe/supabase/010_secure_initial_admin_bootstrap.sql',
              'today-pe/supabase/011_student_privacy_minimization.sql',
              'today-pe/supabase/012_teacher_approval_context.sql',
              'today-pe/supabase/013_teacher_access_revocation.sql',
              'today-pe/supabase/014_protect_school_admin_revocation.sql',
              'api/timetable.js', 'api/schools.js', 'api/public-config.js', 'api/health.js',
              'api/push/subscribe.js', 'api/push/send.js',
              'tests/oneul-pe.spec.js', 'playwright.config.js', 'vercel.json', '.env.example', 'package.json',
          ]
          for f in required:
              if not Path(f).is_file(): raise SystemExit(f'Missing required file: {f}')

          login_html = Path('today-pe/teacher-login.html').read_text(encoding='utf-8')
          if 'teacher-approval-context.js' not in login_html:
              raise SystemExit('teacher-login.html missing approval context adapter')
          teacher_html = Path('today-pe/teacher.html').read_text(encoding='utf-8')
          for script in ('teacher-session-guard.js', 'teacher-admin-protection.js'):
              if script not in teacher_html:
                  raise SystemExit(f'teacher.html missing {script}')
          student_html = Path('today-pe/student.html').read_text(encoding='utf-8')
          if student_html.index('student-privacy.js') > student_html.index('student-resilience.js'):
              raise SystemExit('student privacy must load before resilience cache')
          print('OK: portal structure')
          PY

      - name: Check security and data boundaries
        run: |
          grep -q "TEACHER_NOT_VERIFIED" today-pe/supabase/schema.sql
          grep -q "operator_promote_school_admin" today-pe/supabase/010_secure_initial_admin_bootstrap.sql
          grep -q "to service_role" today-pe/supabase/010_secure_initial_admin_bootstrap.sql
          grep -q "get_my_teacher_approval_context" today-pe/supabase/012_teacher_approval_context.sql
          grep -q "approvalRoute" today-pe/supabase/012_teacher_approval_context.sql
          grep -q "pending_initial_admin" today-pe/teacher-login.js
          grep -q "revoke_teacher_access" today-pe/supabase/013_teacher_access_revocation.sql
          grep -q "CANNOT_REVOKE_SELF" today-pe/supabase/013_teacher_access_revocation.sql
          grep -q "CANNOT_REVOKE_ADMIN" today-pe/supabase/014_protect_school_admin_revocation.sql
          grep -q "v_target.role = 'school_admin'" today-pe/supabase/014_protect_school_admin_revocation.sql
          grep -q "관리자 계정 · 운영자만 변경" today-pe/teacher-admin-protection.js
          grep -q "data-revoke-teacher" today-pe/teacher-dashboard-controls.js
          grep -q "setInterval(checkTeacherAccess, 60_000)" today-pe/teacher-session-guard.js
          grep -q "getTeacherSession" today-pe/teacher-session-guard.js
          grep -q "teacher_timetable_aliases" today-pe/supabase/009_teacher_timetable_aliases.sql
          grep -q "컴시간 이름 매칭 설정" today-pe/teacher-dashboard-controls.js
          grep -q "student-privacy.js" today-pe/student.html
          grep -q "teacherIds: \[\]" today-pe/student-privacy.js
          grep -q "changedByName: ''" today-pe/student-privacy.js
          if grep -q "'teacher_name'" today-pe/supabase/011_student_privacy_minimization.sql; then exit 1; fi
          if grep -q "'teacher_id'" today-pe/supabase/011_student_privacy_minimization.sql; then exit 1; fi
          echo 'OK: auth, approval, revocation, admin protection, aliases and student privacy boundaries'

      - name: Check collaboration, PWA and deployment wiring
        run: |
          grep -q "setLessonTeachers" today-pe/backend.js
          grep -q "listLessonHistory" today-pe/backend.js
          grep -q "listStudentNotifications" today-pe/backend.js
          grep -q "serviceWorker.register" today-pe/pwa.js
          grep -q "pushManager.subscribe" today-pe/pwa.js
          grep -q "self.addEventListener('push'" today-pe/sw.js
          grep -q "student-privacy.js" today-pe/sw.js
          grep -q "teacher-session-guard.js" today-pe/sw.js
          grep -q "teacher-approval-context.js" today-pe/sw.js
          grep -q "teacher-admin-protection.js" today-pe/sw.js
          grep -q "oneul-pe-v6" today-pe/sw.js
          grep -q "SUPABASE_SERVICE_ROLE_KEY" api/push/send.js
          grep -q "VAPID_PRIVATE_KEY" api/push/send.js
          grep -q "/api/schools" today-pe/shared.js
          grep -q "if (process.env.SUPABASE_URL)" api/public-config.js
          grep -q "supabasePublicFallback" api/health.js
          grep -q '"/student"' vercel.json
          echo 'OK: collaboration + PWA + deployment wiring'

      - name: Check timetable provider priority
        run: |
          grep -q "fromComcigan" api/timetable.js
          grep -q "fromNeis" api/timetable.js
          grep -q "await fromComcigan" api/timetable.js
          grep -q "await fromNeis" api/timetable.js
          grep -q "/api/timetable" today-pe/teacher.js
          echo 'OK: Comcigan primary -> NEIS fallback'

      - name: Serve static site and smoke test
        run: |
          python3 -m http.server 4173 --directory . >/tmp/oneul-pe-http.log 2>&1 &
          server_pid=$!
          trap 'kill $server_pid' EXIT
          sleep 1
          for page in index.html student.html teacher-login.html teacher.html; do
            curl --fail --silent "http://127.0.0.1:4173/today-pe/$page" | grep -q '오늘체육'
          done
          for file in shared.js backend.js student-privacy.js teacher-approval-context.js teacher-session-guard.js teacher-admin-protection.js pwa.js sw.js; do
            curl --fail --silent "http://127.0.0.1:4173/today-pe/$file" >/dev/null
          done
          echo 'OK: static smoke test'

      - name: Install Playwright browser
        run: npx playwright install --with-deps chromium

      - name: Browser E2E student-teacher flow
        run: |
          python3 -m http.server 4173 --directory . >/tmp/oneul-pe-e2e-http.log 2>&1 &
          server_pid=$!
          trap 'kill $server_pid' EXIT
          sleep 1
          npm run test:e2e

      - name: Check live Comcigan school search
        continue-on-error: true
        run: |
          node --input-type=module - <<'NODE'
          import Comcigan from 'parse-comcigan';
          const schools = await Comcigan.search('대덕소프트웨어마이스터고');
          console.log('Comcigan schools:', schools.length);
          if (!schools.length) process.exitCode = 1;
          NODE

      - name: Check NEIS school search response
        continue-on-error: true
        run: |
          python3 - <<'PY'
          import json, urllib.parse, urllib.request
          query = urllib.parse.urlencode({'Type':'json','pIndex':'1','pSize':'5','SCHUL_NM':'대덕소프트웨어마이스터고'})
          with urllib.request.urlopen('https://open.neis.go.kr/hub/schoolInfo?' + query, timeout=10) as response:
              data = json.load(response)
          rows = (data.get('schoolInfo') or [{}, {'row': []}])[1].get('row', []) if data.get('schoolInfo') else []
          print('NEIS rows:', len(rows))
          if rows: print('First school:', rows[0].get('SCHUL_NM'))
          PY
